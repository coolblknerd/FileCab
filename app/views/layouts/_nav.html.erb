<% if user_signed_in? %>
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "FileCab", root_path, class: "navbar-brand" %>
    </div>

    <!-- Navbar left -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li></li>
        <li></li>
      </ul>

      <!-- Navbar right -->
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to new_document_path do %>
          <i class="glyphicon glyphicon-file"> Create-Doc</i>
          <% end %>
        </li>
        <li>
          <%= link_to edit_user_registration_path do %>
          <i class="glyphicon glyphicon-user"> Edit-Profile</i>
          <% end %>
        </li>
        <li>
          <%= link_to destroy_user_session_path, method: :delete, data: { confirm: "Are you ready to leave?"} do %>
          <i class="glyphicon glyphicon-log-out"> Log-Out</i>
          <% end %>
        </li>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<% end %>
